# Dolce Pasta — Astro Site

## Setup
```
npm install
npm run dev       # http://localhost:4321
npm run build     # outputs to dist/
npm run preview   # preview the production build
```

## Still needs before go-live
- Real prices for pasta/pizza/steak mains (currently shown as "Market Price" — I was only given prices for the Thai food and dessert menu pages)
- Phone number (not supplied)
- Reservation form has no backend — wire it to email, a booking API, or LINE before launch
- Real Instagram/Facebook/LINE URLs are placeholder `#` links — swap in actual profile URLs
